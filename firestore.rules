rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions (Admin SDK), which bypasses
    // these rules. No direct client reads/writes are allowed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
